export interface LogoProps {
  // 是否折叠状态
  collapsed?: boolean;
  // 高度
  height?: number;
  // logo图片地址
  src?: string;
  // 系统名称
  text?: string;
  // 主题 light | dark
  theme?: string;
  // 自定义类名
  class?: string;
}
